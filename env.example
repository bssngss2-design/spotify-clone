# FastAPI base URL for Next.js rewrites (server-side). Match the port you run uvicorn on.
BACKEND_URL=http://127.0.0.1:8080

# Optional: browser calls this host directly instead of same-origin /api (skips rewrites).
# Leave unset so login uses /api → BACKEND_URL.
# NEXT_PUBLIC_API_URL=
